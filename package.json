{
    "name": "public_html",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "devDependencies": {
        "@types/bun": "latest",
        "sharp": "^0.34.1"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "scripts": {
        "build": "bun build --outdir=dist --minify --target=browser --no-sourcemap",
        "start": "bun run build && bun serve dist/index.js",
        "generate-icons": "bun run ./aura/icons/generate-icons.ts"
    },
    "prettier": {
        "singleQuote": true,
        "semi": false,
        "trailingComma": "all",
        "tabWidth": 4,
        "printWidth": 120
    },
    "dependencies": {
        "lit": "^3.3.0",
        "prettier": "^3.5.3"
    }
}
